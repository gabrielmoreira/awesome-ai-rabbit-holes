# NOT AN INPUT FILE.
# The only catalog intake is config/sources.yml.
# This legacy path is intentionally retained as an empty pointer so contributors
# do not accidentally create a second queue. Add tool submissions to
# config/sources.yml and let the scheduled automation process them.
[]