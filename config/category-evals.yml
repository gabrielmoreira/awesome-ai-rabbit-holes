# ─────────────────────────────────────────────────────────────────────────────
# coding-agents
# ─────────────────────────────────────────────────────────────────────────────

- id: coding-agents__opencode
  item_id: github__anomalyco__opencode
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: coding-agents
    section: Terminal & CLI Agents
  hard_negatives:
    - app-builders
    - mcp
  notes: Terminal-native coding assistant; primary identity is the coding task, not generating apps.

- id: coding-agents__cline
  item_id: github__cline__cline
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: coding-agents
    section: IDE-Native & Editor Agents
  hard_negatives:
    - extensions-and-addons
    - mcp
  notes: >
    IDE-hosted coding assistant. The agent IS the product, not an add-on to another
    agent — keep in coding-agents despite living inside VS Code.

- id: coding-agents__ui-tars-desktop
  item_id: github__bytedance__ui-tars-desktop
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: coding-agents
    section: Browser & Computer-Use Agents
  hard_negatives:
    - mcp
    - app-builders
  notes: Computer-use agent for developers; browser/desktop control is the coding mechanism, not an app generator.

- id: coding-agents__coderabbit
  item_id: coderabbit__ai
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: coding-agents
    section: Review, Testing & Repair
  hard_negatives:
    - evals
    - extensions-and-addons
  notes: >
    PR review agent whose job is finding and explaining code issues. Belongs in coding-agents
    review section, not evals (which measures model behavior) or extensions (not a host add-on).

- id: coding-agents__cursor-ide
  item_id: cursor__com
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: coding-agents
    section: IDE-Native & Editor Agents
  hard_negatives:
    - extensions-and-addons
    - agent-orchestration
  notes: >
    AI-first editor product — the IDE surface IS the agent. After dissolving ai-ides-editors,
    this lands in coding-agents/IDE-Native & Editor Agents. Not an extension because Cursor
    is the primary environment, not an add-on to another editor.

- id: coding-agents__kiro
  item_id: kiro__dev
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: coding-agents
    section: Spec-First & Plan-Execute
  hard_negatives:
    - app-builders
    - ai-frameworks
    - extensions-and-addons
  notes: >
    Spec-driven IDE/agent environment. After dissolving spec-driven-development category,
    Kiro belongs in coding-agents/Spec-First & Plan-Execute — specs are the input method for
    coding tasks, not a separate product domain.

- id: coding-agents__spec-kit
  item_id: github__github__spec-kit
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: coding-agents
    section: Spec-First & Plan-Execute
  hard_negatives:
    - extensions-and-addons
    - skills
  notes: >
    Spec-first toolkit and slash-command workflow. The output of using spec-kit is working code
    via an agent — coding-agents/Spec-First. Not a skill pack (skills are installable content
    packs, not spec-execution environments).

- id: coding-agents__openwork
  item_id: github__different-ai__openwork
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: coding-agents
    section: Browser & Computer-Use Agents
  hard_negatives:
    - agent-orchestration
    - ai-frameworks
  notes: Local-first computer-use coding environment. Primary identity is the coding surface, not an orchestration control plane.

- id: coding-agents__aider
  item_id: github__aider-ai__aider
  suite: prompt-holdout
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: coding-agents
    section: Terminal & CLI Agents
  hard_negatives:
    - ai-frameworks
    - extensions-and-addons
  notes: >
    CLI coding agent that edits files and runs tests. A framework would be imported as a library;
    Aider is invoked directly by the developer to do coding work.

- id: coding-agents__babyagi-multi
  item_id: github__yoheinakajima__babyagi
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: coding-agents
    section: Autonomous & Multi-Agent Coding
  hard_negatives:
    - agent-orchestration
    - ai-frameworks
  notes: >
    Autonomous task-decomposition coding agent. The orchestration is a means to writing code,
    not the product identity. Stays in coding-agents/Autonomous & Multi-Agent Coding rather than
    agent-orchestration (which coordinates agents for general workflows, not specifically coding).

# ─────────────────────────────────────────────────────────────────────────────
# app-builders
# ─────────────────────────────────────────────────────────────────────────────

- id: app-builders__bolt
  item_id: bolt__new
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: app-builders
    section: Full App Builders
  hard_negatives:
    - coding-agents
    - extensions-and-addons
  notes: Hosted prompt-to-full-stack-app surface. Output is a deployable app; the developer starts from zero.

- id: app-builders__10web
  item_id: 10web__io
  suite: prompt-holdout
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: app-builders
    section: Site & Landing Page Builders
  hard_negatives:
    - coding-agents
    - agent-orchestration
  notes: AI-powered website builder that generates and deploys sites from text prompts. Output is a published site.

- id: app-builders__wren-ai
  item_id: github__canner-inc__getwrenai
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: app-builders
    section: Internal Tools & Dashboards
  hard_negatives:
    - evals
    - agent-orchestration
  notes: >
    GenBI platform that generates governed natural-language SQL interfaces and dashboards.
    The output is an internal data tool, not a general app — lands in Internal Tools & Dashboards.
    Not evals (doesn't measure model behavior) and not orchestration (no multi-agent coordination).

# ─────────────────────────────────────────────────────────────────────────────
# extensions-and-addons  (was ai-dev-extensions)
# ─────────────────────────────────────────────────────────────────────────────

- id: extensions-and-addons__claude-debugs-for-you
  item_id: github__jasonjmcghee__claude-debugs-for-you
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: extensions-and-addons
    section: IDE & Editor Add-ons
  hard_negatives:
    - coding-agents
    - mcp
  notes: >
    VS Code debugger bridge for Claude. The host is VS Code; this product augments the
    debugging workflow. Even though it ships an MCP server, the product identity is the
    IDE add-on, not MCP infrastructure.

- id: extensions-and-addons__git-ai
  item_id: github__git-ai-project__git-ai
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: extensions-and-addons
    section: IDE & Editor Add-ons
  hard_negatives:
    - coding-agents
    - agent-orchestration
  notes: Git workflow attribution tooling that augments existing coding workflows.

- id: extensions-and-addons__task-master
  item_id: github__eyaltoledano__claude-task-master
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: extensions-and-addons
    section: Claude Code & Agent Tools
  hard_negatives:
    - coding-agents
    - mcp
  notes: >
    Workflow and task management layer that operates inside Claude Code. Claude Code is still
    the primary agent; task-master extends it. Belongs in Claude Code & Agent Tools, not
    coding-agents (it doesn't code independently) and not mcp (MCP is the transport, not the identity).

- id: extensions-and-addons__forge
  item_id: github__lucasduys__forge
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: extensions-and-addons
    section: Claude Code & Agent Tools
  hard_negatives:
    - coding-agents
    - agent-orchestration
  notes: Packages spec-driven automation as a Claude Code workflow extension. Host integration identity wins over spec-platform or standalone agent.

- id: extensions-and-addons__figma-use
  item_id: github__dannote__figma-use
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: extensions-and-addons
    section: API & Service Integrations
  hard_negatives:
    - coding-agents
    - mcp
  notes: Figma automation integration for coding agents. Adds design-to-code capability to an existing workflow; not itself an agent and not MCP infrastructure.

- id: extensions-and-addons__tailtest-cline
  item_id: github__avansaber__tailtest-cline
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: extensions-and-addons
    section: Testing & Code Quality
  hard_negatives:
    - coding-agents
    - mcp
  notes: QA/testing booster for Cline. Augments an existing agent with testing capability — not standalone and not MCP.

- id: extensions-and-addons__checkmarx
  item_id: checkmarx__com
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: extensions-and-addons
    section: Security & DevSecOps
  hard_negatives:
    - evals
    - coding-agents
  notes: >
    Agentic application security platform that integrates into coding workflows. Security scanning
    is the add-on capability; it augments the development pipeline. Not evals (doesn't grade model
    behavior) and not a standalone coding agent.

# ─────────────────────────────────────────────────────────────────────────────
# mcp
# ─────────────────────────────────────────────────────────────────────────────

- id: mcp__playwright
  item_id: github__executeautomation__mcp-playwright
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: mcp
    section: Browser & Web Automation
  hard_negatives:
    - coding-agents
    - extensions-and-addons
  notes: Browser automation MCP server. Identity is the MCP server providing browser tools, not a coding agent or host add-on.

- id: mcp__github-server
  item_id: github__github__github-mcp-server
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: mcp
    section: Dev & Code Tools
  hard_negatives:
    - coding-agents
    - extensions-and-addons
  notes: >
    Official GitHub MCP server exposing repos, issues, and PRs over the protocol.
    Identity is MCP infrastructure for developer tooling. Not a coding agent
    (doesn't execute code) and not an extension (not a plugin inside another tool).

- id: mcp__registry
  item_id: github__modelcontextprotocol__registry
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: mcp
    section: Registries & Discovery
  hard_negatives:
    - agent-orchestration
    - awesome-awesomes
  notes: >
    Official MCP registry/API backbone for server discovery. MCP infrastructure identity
    beats the "list of things" interpretation — it is not a static awesome list and not
    an orchestration platform.

- id: mcp__fastmcp
  item_id: github__jlowin__fastmcp
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: mcp
    section: Clients & Inspector Tools
  hard_negatives:
    - ai-frameworks
    - extensions-and-addons
  notes: >
    High-level Python SDK and framework for building MCP servers. Identity is MCP
    infrastructure/tooling, not a general agent framework (ai-frameworks) and not
    an extension of another tool.

- id: mcp__finance-server
  item_id: github__berlinbaconsurgeon__bitcoin-mcp
  suite: prompt-holdout
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: mcp
    section: Finance, Crypto & DeFi
  hard_negatives:
    - ai-frameworks
    - coding-agents
  notes: "Bitcoin/crypto data MCP server. Domain MCP server: gives agents access to crypto data. Not a framework and not a coding agent."

- id: mcp__database-server
  item_id: github__benborla29__mcp-server-mysql
  suite: prompt-holdout
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: mcp
    section: Data & Databases
  hard_negatives:
    - ai-frameworks
    - memory-and-context
  notes: >
    MySQL MCP server exposing database operations to agents. This is MCP domain infrastructure,
    not a retrieval/memory system (memory-and-context) and not a code library (ai-frameworks).

# ─────────────────────────────────────────────────────────────────────────────
# agent-orchestration
# ─────────────────────────────────────────────────────────────────────────────

- id: agent-orchestration__agentsmesh
  item_id: github__agentsmesh__agentsmesh
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: agent-orchestration
    section: Hosted Agent Platforms
  hard_negatives:
    - coding-agents
    - ai-frameworks
  notes: AI agent workforce platform for software teams. Coordinates squads of agents; the coordination product is the identity.

- id: agent-orchestration__langflow
  item_id: github__langflow-ai__langflow
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: agent-orchestration
    section: Visual Builders & Workbenches
  hard_negatives:
    - ai-frameworks
    - coding-agents
  notes: >
    Visual drag-and-drop workflow builder for multi-agent systems. Despite being built on
    LangChain (a framework), LangFlow itself is an operated visual builder — agent-orchestration
    identity wins over ai-frameworks. Users run it, they don't import it.

- id: agent-orchestration__amux
  item_id: github__bhunjika__amux
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: agent-orchestration
    section: Agent Runtimes & Harnesses
  hard_negatives:
    - coding-agents
    - extensions-and-addons
  notes: >
    Terminal UI for running multiple coding agents in parallel with workspace isolation.
    The product manages multiple agents simultaneously — orchestration runtime identity.
    Not a single coding agent and not an extension of another tool.

# ─────────────────────────────────────────────────────────────────────────────
# ai-frameworks
# ─────────────────────────────────────────────────────────────────────────────

- id: ai-frameworks__crewai
  item_id: github__crewaiinc__crewai
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: ai-frameworks
    section: Multi-Agent Systems
  hard_negatives:
    - agent-orchestration
    - coding-agents
  notes: >
    Code-first Python multi-agent framework. Despite orchestration-heavy marketing, the
    product identity is the importable library — developers write CrewAI code. Belongs in
    ai-frameworks/Multi-Agent Systems, not the operated agent-orchestration platforms.

- id: ai-frameworks__phidata
  item_id: github__agno-agi__phidata
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: ai-frameworks
    section: LLM & Agent SDKs
  hard_negatives:
    - agent-orchestration
    - coding-agents
  notes: Framework-first despite orchestration-heavy discovery context. Developers import Agno/Phidata as a library.

- id: ai-frameworks__langgraph
  item_id: github__langchain-ai__langgraph
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: ai-frameworks
    section: Workflow & Graph Engines
  hard_negatives:
    - agent-orchestration
    - coding-agents
  notes: >
    Stateful graph-based workflow engine for multi-actor applications. Developers write Python
    against its API — it is not an operated platform. Graph engine sub-category of ai-frameworks,
    not agent-orchestration (which is for run-it platforms).

# ─────────────────────────────────────────────────────────────────────────────
# evals
# ─────────────────────────────────────────────────────────────────────────────

- id: evals__agenta
  item_id: github__agenta-ai__agenta
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: evals
    section: Observability & Tracing
  hard_negatives:
    - agent-orchestration
    - ai-frameworks
  notes: Prompt and eval platform; dominant section is eval/observability ops. Not an orchestration platform.

- id: evals__agentbench
  item_id: github__agentbench__agentbench
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: evals
    section: Agent & LLM Benchmarks
  hard_negatives:
    - agent-orchestration
    - coding-agents
  notes: Benchmark harness for evaluating agent capabilities. Measures agents — does not run them as a product.

- id: evals__promptfoo
  item_id: github__promptfoo__promptfoo
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: evals
    section: Prompt Regression & Testing
  hard_negatives:
    - extensions-and-addons
    - memory-and-context
  notes: >
    Open-source LLM testing and red-teaming tool. Primary job is prompt regression testing
    and evaluation — evals identity. Not an extension (standalone tool) and not a context
    management system.

- id: evals__langfair
  item_id: github__cvs-health__langfair
  suite: prompt-holdout
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: evals
    section: Safety, Fairness & Red-teaming
  hard_negatives:
    - ai-frameworks
    - agent-orchestration
  notes: Python library for LLM bias and fairness assessment. Measures safety properties of model outputs.

# ─────────────────────────────────────────────────────────────────────────────
# local-ai
# ─────────────────────────────────────────────────────────────────────────────

- id: local-ai__llama-cpp-python
  item_id: github__abetlen__llama-cpp-python
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: local-ai
    section: Local API Servers
  hard_negatives:
    - ai-frameworks
    - coding-agents
  notes: Python bindings for llama.cpp providing an OpenAI-compatible local server. Local execution identity beats the framework interpretation.

- id: local-ai__open-webui
  item_id: github__open-webui__open-webui
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: local-ai
    section: Desktop Chat & UIs
  hard_negatives:
    - agent-orchestration
    - coding-agents
  notes: >
    Open-source web UI for running local models. Self-hosted chat interface — local-ai
    identity. Despite supporting multi-model routing and some agent features, the primary
    value is the UI for local/self-hosted inference, not orchestrating agent workflows.

- id: local-ai__unsloth
  item_id: github__unslothai__unsloth
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: local-ai
    section: Fine-tuning & Training
  hard_negatives:
    - ai-frameworks
    - evals
  notes: >
    Fast fine-tuning library for LLMs on local hardware. Local execution is the product
    identity — users run fine-tuning jobs on their own GPUs. Not a general agent framework
    (purpose is fine-tuning specifically) and not an eval tool.

# ─────────────────────────────────────────────────────────────────────────────
# memory-and-context  (was context-engineering)
# ─────────────────────────────────────────────────────────────────────────────

- id: memory-and-context__context-memory
  item_id: github__erebusenigma__context-memory
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: memory-and-context
    section: Persistent Memory & Knowledge
  hard_negatives:
    - extensions-and-addons
    - mcp
  notes: >
    Claude Code memory layer that bootstraps a knowledge base from git history.
    Even though it integrates with Claude Code, the core novelty is the persistent memory
    system itself, not the host integration layer.

- id: memory-and-context__cortex
  item_id: github__cdeust__cortex
  suite: prompt-core
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: memory-and-context
    section: Persistent Memory & Knowledge
  hard_negatives:
    - extensions-and-addons
    - mcp
  notes: Memory/retrieval infrastructure with benchmarks. The memory system is the product; it beats the thin host-addon interpretation.

- id: memory-and-context__context7
  item_id: context7__com
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: memory-and-context
    section: Retrieval & Fresh Docs
  hard_negatives:
    - coding-agents
    - mcp
  notes: Fresh-doc retrieval service that surfaces current library documentation for agents. The retrieval/context system is the product.

- id: memory-and-context__hypersigil
  item_id: github__hypersigilhq__hypersigil
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: memory-and-context
    section: Prompt Engineering & Management
  hard_negatives:
    - ai-frameworks
    - evals
  notes: Prompt lifecycle and hot-swap infrastructure. Prompt management is the product; stays out of framework or eval buckets.

- id: memory-and-context__promptsource
  item_id: github__bigscience-workshop__promptsource
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: memory-and-context
    section: Prompt Engineering & Management
  hard_negatives:
    - coding-agents
    - extensions-and-addons
  notes: Prompt dataset creation and management toolkit. Prompt asset management identity — not a coding assistant and not a host add-on.

- id: memory-and-context__mem0
  item_id: github__mem0ai__mem0
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: memory-and-context
    section: Persistent Memory & Knowledge
  hard_negatives:
    - ai-frameworks
    - extensions-and-addons
  notes: >
    Persistent memory layer for AI agents across sessions. Though it has an SDK, the product
    identity is the memory system, not a general agent framework. The memory is the product.

- id: memory-and-context__claude-engram
  item_id: github__aaronvick__claude-engram
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: memory-and-context
    section: Context & Session Management
  hard_negatives:
    - extensions-and-addons
    - mcp
  notes: >
    MCP server providing persistent session memory and context handoff for Claude Code.
    The MCP transport does not override the identity: this product IS the memory/context
    management system, not an extension add-on and not a generic MCP server.

# ─────────────────────────────────────────────────────────────────────────────
# skills
# ─────────────────────────────────────────────────────────────────────────────

- id: skills__anthropics-skills
  item_id: github__anthropics__skills
  suite: prompt-holdout
  fix_scope: mixed
  case_type: adversarial
  expected:
    include: true
    primary_category: skills
    section: Skill Packs & Libraries
  hard_negatives:
    - mcp
    - extensions-and-addons
    - coding-agents
  notes: Official Anthropic skills repository. The reusable skill content itself is the product — installable, portable, content-first.

- id: skills__skills-sh
  item_id: skills__sh
  suite: prompt-holdout
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: skills
    section: Registries & Directories
  hard_negatives:
    - extensions-and-addons
    - awesome-awesomes
    - coding-agents
  notes: Skills marketplace and directory for AI agents. The registry identity makes it skills, not awesome-awesomes (which is for general tool lists).

- id: skills__qovery-skills
  item_id: github__qovery__qovery-skills
  suite: prompt-holdout
  fix_scope: prompt
  case_type: adversarial
  expected:
    include: true
    primary_category: skills
    section: Skill Packs & Libraries
  hard_negatives:
    - extensions-and-addons
    - coding-agents
    - mcp
  notes: Domain-specific deployment skill pack for Claude Code. Installable skill content — not an executable extension and not MCP infrastructure.

# ─────────────────────────────────────────────────────────────────────────────
# awesome-awesomes
# ─────────────────────────────────────────────────────────────────────────────

- id: awesome-awesomes__coding-tools
  item_id: github__ai-for-developers__awesome-ai-coding-tools
  suite: prompt-core
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: awesome-awesomes
    section: Awesome Lists & Directories
  hard_negatives:
    - coding-agents
    - app-builders
  notes: Curated index of AI coding tools. Curation and discovery is the product — not a tool itself.

- id: awesome-awesomes__ai-for-developers-newsletter
  item_id: aifordevelopers__substack__com
  suite: prompt-holdout
  fix_scope: prompt
  case_type: positive
  expected:
    include: true
    primary_category: awesome-awesomes
    section: Publications & Feeds
  hard_negatives:
    - coding-agents
    - memory-and-context
  notes: >
    Recurring developer newsletter about AI tools. After dissolving newsletters-and-feeds
    category, publications move into awesome-awesomes/Publications & Feeds.
    Commentary and curation are the value, not any runtime tool.

# ─────────────────────────────────────────────────────────────────────────────
# pipeline-redflags  (negative cases — should never be included)
# ─────────────────────────────────────────────────────────────────────────────

- id: redflag__devdocs_aux_link
  item_id: devdocs__io
  suite: pipeline-redflags
  fix_scope: deterministic
  case_type: negative
  expected:
    include: false
    primary_category: null
    section: null
  hard_negatives:
    - app-builders
    - coding-agents
    - mcp
  notes: Auxiliary link extracted from an MCP source-list line. Developer documentation reference, not a catalogable product.

- id: redflag__copilot_docs_page
  item_id: code__visualstudio__com__docs__copilot__customization__overview
  suite: pipeline-redflags
  fix_scope: deterministic
  case_type: negative
  expected:
    include: false
    primary_category: null
    section: null
  hard_negatives:
    - coding-agents
    - extensions-and-addons
  notes: Official VS Code documentation page for Copilot customization. A docs page is not a standalone catalogable product.

- id: redflag__github_docs_page
  item_id: docs__github__com
  suite: pipeline-redflags
  fix_scope: deterministic
  case_type: negative
  expected:
    include: false
    primary_category: null
    section: null
  hard_negatives:
    - coding-agents
    - mcp
    - awesome-awesomes
  notes: Generic GitHub documentation domain. Not a product — any item extracted from docs.github.com should be rejected as an auxiliary link.
